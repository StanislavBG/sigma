# Data — the procurement corpus is hosted on Replit persistent storage / Postgres,
# NEVER committed to git (the served SQLite is ~1.7 GB). See replit.md.
data/
data-runtime/
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.react-router/
.turbo/
.vite/
coverage/

# Cloudflare / wrangler
.wrangler/
.dev.vars
wrangler.local.toml
# rendered by scripts/wrangler-render.mjs (committed wrangler.* hold zero-UUID dummies)
apps/*/wrangler.deploy.toml
apps/*/wrangler.deploy.jsonc

# Env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.local
*.log

# Claude Code per-user settings — local overrides (e.g. bypassPermissions) stay on the machine
.claude/settings.local.json

# TypeScript
*.tsbuildinfo

# Cloudflare Workers generated types (regenerated by `wrangler types`)
worker-configuration.d.ts
.codex-workers/
.worktrees/

# Local-only policy/regulatory source materials copied in for review (never commit)
.policy-source/
